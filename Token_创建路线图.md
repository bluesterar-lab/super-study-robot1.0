# 🗺️ GitHub Token 创建 - 路线图

## 📍 从 GitHub 首页开始的完整路径

```
1. https://github.com
   ↓ (点击头像)
2. Settings
   ↓ (左侧菜单最底部)
3. Developer settings
   ↓ (左侧菜单)
4. Personal access tokens
   ↓ (子菜单)
5. Tokens (classic)
   ↓ (右上角)
6. Generate new token (classic)
   ↓ (填写表单)
7. Generate token
   ↓ (复制 Token)
8. 完成！
```

---

## 🎯 每一步的详细位置

### 步骤 1：登录 GitHub
```
地址：https://github.com
操作：登录你的账号 bluesterar-lab
```

### 步骤 2：进入设置
```
位置：右上角
操作：
  1. 点击头像（右上角的圆形头像）
  2. 点击 "Settings"（可能是齿轮图标 ⚙️）
```

### 步骤 3：开发者设置
```
位置：左侧菜单最底部
操作：
  1. 向下滚动左侧菜单
  2. 找到 "Developer settings"
  3. 点击进入
```

### 步骤 4：访问令牌菜单
```
位置：Developer settings 左侧菜单
操作：
  1. 找到 "Personal access tokens"
  2. 展开这个菜单
  3. 点击 "Tokens (classic)"
```

### 步骤 5：生成新令牌
```
位置：页面右上角
操作：
  1. 找到 "Generate new token (classic)" 按钮
  2. 点击这个按钮（通常是绿色）
```

### 步骤 6：配置令牌
```
Note（备注）：
  输入：git-token（或其他名称）

Expiration（过期时间）：
  选择：90 days

Scopes（权限）：
  找到 "repo" 部分
  勾选：repo（整个复选框）
```

### 步骤 7：生成并复制
```
操作：
  1. 滚动到页面底部
  2. 点击 "Generate token" 按钮
  3. 立即复制显示的 Token
  4. 保存到记事本
```

---

## 🎨 界面说明

### Settings 页面应该看到：
```
┌─────────────────────────────────────┐
│  Header (Logo, 搜索, 头像)          │
├─────────────────────────────────────┤
│  左侧菜单                            │
│  ┌─────────────────────────────┐   │
│  │ Profile                      │   │
│  │ Account                      │   │
│  │ Emails                       │   │
│  │ ...                          │   │
│  │ Developer settings ⬇️        │   │  ← 找这里！
│  └─────────────────────────────┘   │
│                                     │
│  右侧设置区域                       │
└─────────────────────────────────────┘
```

### Developer settings 页面应该看到：
```
┌─────────────────────────────────────┐
│  Developer settings                 │
├─────────────────────────────────────┤
│  左侧菜单                            │
│  ┌─────────────────────────────┐   │
│  │ OAuth Apps                  │   │
│  │ GitHub Apps                 │   │
│  │ Personal access tokens ⬇️    │   │  ← 找这里！
│  │   └─ Tokens (classic) ⬇️    │   │  ← 点击这个！
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### Tokens (classic) 页面应该看到：
```
┌─────────────────────────────────────┐
│  Personal access tokens (classic)   │
├─────────────────────────────────────┤
│                                     │
│  Generate new token (classic)       │  ← 点击这个按钮！
│  [   点击这里   ]                    │
│                                     │
│  已有的 Token 列表...               │
└─────────────────────────────────────┘
```

### Token 创建表单：
```
┌─────────────────────────────────────┐
│  New personal access token          │
├─────────────────────────────────────┤
│  Note: [git-token               ]   │
│  Expiration: [90 days            ]   │
│                                     │
│  Scopes（权限）                      │
│  ┌─────────────────────────────┐   │
│  │ ☐ repo                       │   │  ← 勾选这里！
│  │   ☐ repo:status              │   │
│  │   ☐ repo_deployment          │   │
│  │   ☐ public_repo              │   │
│  │   ☐ repo:invite              │   │
│  │   ☐ security_events          │   │
│  └─────────────────────────────┘   │
│                                     │
│  [Generate token]                   │  ← 点击这里！
└─────────────────────────────────────┘
```

---

## 🔍 快速定位方法

### 使用浏览器查找功能：

**在 Settings 页面**：
1. 按 `Ctrl+F`（Mac: `Cmd+F`）
2. 输入：`Developer`
3. 按回车，会高亮显示

**在 Developer settings 页面**：
1. 按 `Ctrl+F`（Mac: `Cmd+F`）
2. 输入：`Tokens`
3. 按回车，会高亮显示

**在 Tokens 页面**：
1. 按 `Ctrl+F`（Mac: `Cmd+F`）
2. 输入：`Generate`
3. 按回车，会高亮显示按钮

---

## 🎯 一键直达链接

### 直接打开 Token 页面：
```
https://github.com/settings/tokens
```

### 如果登录了，会直接显示：
- 已有的 Token 列表
- "Generate new token (classic)" 按钮

### 如果没登录：
- 会跳转到登录页面
- 登录后会自动跳转回来

---

## ✅ 成功的标志

当你看到这个页面，说明找到了！

```
Personal access tokens (classic)

Generate new token (classic)
[ 点击这里 ]
```

然后点击按钮，填写表单，生成 Token！

---

## 📊 对比检查

| 步骤 | 应该看到 | 如果你看到 |
|------|---------|-----------|
| 1 | GitHub 首页 | 登录页面 → 先登录 |
| 2 | Settings 页面 | 仓库页面 → 点头像 |
| 3 | Developer settings | 账号设置 → 滚到底部 |
| 4 | Tokens (classic) | 其他页面 → 找 Personal access tokens |
| 5 | 创建按钮 | Token 列表 → 点右上角 |
| 6 | 填写表单 | 其他内容 → 找 Generate 按钮 |
| 7 | 显示 Token | 表单 → 点 Generate token |
| 8 | 成功！ | 重复上述步骤 |

---

## 🚀 现在开始！

1. 打开浏览器
2. 访问：https://github.com/settings/tokens
3. 点击 "Generate new token (classic)"
4. 按照步骤操作

5 分钟搞定！🎉

---

## 💡 最后的提示

- **找不到按钮？** - 按 `Ctrl+F` 搜索 "Generate"
- **找不到菜单？** - 向下滚动查看
- **页面不一样？** - 可能是 GitHub 更新了，但功能应该在类似位置

---

**现在就去创建 Token 吧！** 💪
