# 🚀 Vercel + GitHub 部署指南（最专业、最稳定）

这是最推荐的部署方式！代码推送后会自动部署，非常方便。

---

## 📋 总共 5 步，10 分钟搞定

### 第 1 步：创建 GitHub 仓库（3分钟）

1. 访问 [https://github.com/new](https://github.com/new)
2. 填写仓库信息：
   - **Repository name**: `happy-learning-platform`（或任意名称）
   - **Description**: 儿童英语数学语文学习平台
   - **Public**: 公开（推荐）或 私有
   - **不要勾选** "Add a README file"
3. 点击 "Create repository"

---

### 第 2 步：推送代码到 GitHub（2分钟）

在你的项目文件夹打开终端/命令行：

```bash
# 添加远程仓库（复制你的仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 推送代码
git branch -M main
git push -u origin main
```

**示例**：
```bash
git remote add origin https://github.com/zhangsan/happy-learning-platform.git
git push -u origin main
```

---

### 第 3 步：登录 Vercel（1分钟）

1. 访问 [https://vercel.com](https://vercel.com)
2. 点击右上角 "Sign Up" 或 "Login"
3. 选择 "Continue with GitHub"
4. 授权 Vercel 访问你的 GitHub

---

### 第 4 步：导入项目到 Vercel（3分钟）

1. 登录后点击 "Add New..." → "Project"
2. 在 "Import Git Repository" 中找到你的仓库
3. 点击 "Import" 按钮
4. 配置项目：
   - **Project Name**: 默认即可
   - **Framework Preset**: Next.js（自动检测）
   - **Build Command**: `pnpm build`（自动填写）
   - **Output Directory**: `.next`（自动填写）
5. 点击 "Deploy" 按钮

---

### 第 5 步：等待部署完成（1-2分钟）

- 看到 "Production: https://your-project-name.vercel.app" 就成功了！
- 点击链接访问你的网站

---

## 🎉 完成！你的网站已经上线了！

---

## 🔄 如何更新网站？

### 超简单！只需 2 步：

**第 1 步：修改代码**
- 编辑任何文件
- 保存修改

**第 2 步：推送到 GitHub**
```bash
git add .
git commit -m "更新了什么内容"
git push
```

**就这么简单！Vercel 会自动重新部署！**

---

## 📝 完整命令速查

```bash
# 1. 初始化 Git 仓库（首次）
git init
git add .
git commit -m "初始提交"

# 2. 添加远程仓库（首次）
git remote add origin https://github.com/用户名/仓库名.git

# 3. 推送代码（首次）
git branch -M main
git push -u origin main

# 4. 更新代码（之后每次）
git add .
git commit -m "描述你的修改"
git push
```

---

## 💡 提示

### 部署后可以做什么？

✅ **查看部署历史**
- 在 Vercel 项目页面点击 "Deployments"

✅ **回滚到旧版本**
- 点击旧版本的 "..." 按钮 → "Promote to Production"

✅ **查看域名**
- 在 Vercel 项目页面点击 "Domains"

✅ **绑定自定义域名**
- 在 "Domains" 页面添加你的域名

✅ **查看环境变量**
- 在 Vercel 项目页面点击 "Settings" → "Environment Variables"

---

## ❓ 常见问题

### Q1: git push 失败？
**A**: 检查以下几点：
```bash
# 检查远程仓库地址
git remote -v

# 如果不对，重新设置
git remote set-url origin https://github.com/用户名/仓库名.git

# 重新推送
git push -u origin main
```

### Q2: Vercel 部署失败？
**A**: 检查以下几点：
1. 查看部署日志（Vercel 项目页面）
2. 确保 `package.json` 中的依赖正确
3. 检查是否有语法错误

### Q3: 如何删除项目？
**A**:
- 在 Vercel 项目页面点击 "Settings" → "Delete Project"
- 在 GitHub 仓库页面点击 "Settings" → "Delete this repository"

### Q4: 如何设置分支？
**A**:
- 在 Vercel 项目页面点击 "Settings" → "Git"
- 选择要部署的分支（如 main、develop）

---

## 🎯 我推荐这种方式的理由

✅ **最稳定** - 代码推送到 Git 才会部署
✅ **最安全** - 有版本控制，可以回滚
✅ **最方便** - 自动部署，不用手动操作
✅ **最专业** - 大公司都用这种方式
✅ **最省心** - 一次配置，永久自动

---

## 🎊 现在就开始吧！

按照上面的 5 步操作，10 分钟就能让网站上线！

**遇到问题随时告诉我！** 💪

---

## 📚 相关文档

- [Vercel 官方文档](https://vercel.com/docs)
- [GitHub 官方文档](https://docs.github.com)
- [Next.js 部署文档](https://nextjs.org/docs/deployment)
